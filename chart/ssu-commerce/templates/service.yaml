apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssu-commerce.fullname" . }}
  labels:
    {{- include "ssu-commerce.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ssu-commerce.selectorLabels" . | nindent 4 }}
